#include <stdio.h>
#include <stdlib.h>

// Define the structure for a circular linked list node
struct Node {
    int data;
    struct Node* next;
};

// Function to allocate memory and create a new node
struct Node* createNode(int data) {
    struct Node* newNode = (struct Node*)malloc(sizeof(struct Node));
    if (newNode == NULL) {
        printf("Memory allocation failed.\n");
        exit(1);
    }
    newNode->data = data;
    newNode->next = NULL;
    return newNode;
}

int main() {
    int n;
    
    // Read the number of elements
    if (scanf("%d", &n) != 1) {
        return 1; 
    }

    // Handle edge case where the list is empty
    if (n <= 0) {
        return 0;
    }

    struct Node* head = NULL;
    struct Node* tail = NULL;

    // Create the circular linked list
    for (int i = 0; i < n; i++) {
        int value;
        scanf("%d", &value);
        
        struct Node* newNode = createNode(value);

        if (head == NULL) {
            // First node: head and tail point to it, and it points to itself
            head = newNode;
            tail = newNode;
            newNode->next = head; 
        } else {
            // Subsequent nodes: attach to tail, update tail, and link back to head
            tail->next = newNode;
            tail = newNode;
            tail->next = head; 
        }
    }

    // Traverse and print the circular linked list
    if (head != NULL) {
        struct Node* current = head;
        
        // A do-while loop is perfect here to ensure we process the head node 
        // before checking the loop continuation condition.
        do {
            printf("%d ", current->data);
            current = current->next;
        } while (current != head);
        
        printf("\n");
    }

    // Clean up dynamically allocated memory to prevent leaks
    if (head != NULL) {
        struct Node* current = head;
        struct Node* next
